apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: todo-frontend-app
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  labels:
    app: todo-frontend
spec:
  project: default
  source:
    repoURL: https://github.com/FaresMirza/todo-app-devops
    targetRevision: main
    path: helm/todo-frontend
  destination:
    server: https://kubernetes.default.svc
    namespace: todo-app
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true